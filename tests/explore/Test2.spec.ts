import test from "@playwright/test";

test('Test 2-1', async({}, testInfo) => {
    console.log(testInfo.workerIndex);
})

test('Test 2-2', async({}, testInfo) => {
    console.log(testInfo.workerIndex);
})

test('Test 2-3', async({}, testInfo) => {
    console.log(testInfo.workerIndex);
})

test('Test 2-4', async({}, testInfo) => {
    console.log(testInfo.workerIndex);
})

test('Test 2-5', async({}, testInfo) => {
    console.log(testInfo.workerIndex);
})

test('Test 2-6', async({}, testInfo) => {
    console.log(testInfo.workerIndex);
})

test('Test 2-7', async({}, testInfo) => {
    console.log(testInfo.workerIndex); 
})

test('Test 2-8', async({}, testInfo) => {
    console.log(testInfo.workerIndex); 
})

test('Test 2-9', async({}, testInfo) => {
    console.log(testInfo.workerIndex); 
})